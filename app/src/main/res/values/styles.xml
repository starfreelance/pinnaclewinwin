<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.GreyRipple">
        <item name="colorControlHighlight">@color/colorStartGold</item>
    </style>

    <style name="AppTheme.BlueRipple">
        <item name="colorControlHighlight">@color/colorDarkBlue</item>
    </style>

    <style name="AppTheme.CoinRipple">
        <item name="colorControlHighlight">@color/colorHTTitle</item>
    </style>

</resources>
